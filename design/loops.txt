// Loops are simple; there are two kinds of loops.
// While loops run while a condition is true.
let running = true
while running {
    println("Running!")
}

// For loops loop through something.
for i in range(1 10) {
    println("Iteration: " .. i)
}